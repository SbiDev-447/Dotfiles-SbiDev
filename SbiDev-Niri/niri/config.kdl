// ============================================================================
//                          NIRI CONFIGURATION
// ============================================================================

// ============================================================================
//                            ENTORNO
// ============================================================================
environment {
    XDG_CURRENT_DESKTOP "niri"
}

// ============================================================================
//                            CURSOR
// ============================================================================
cursor {
    xcursor-theme "Win7OS-cursors"
    xcursor-size 40
}

// ============================================================================
//                              INPUTS
// ============================================================================
input {
    keyboard {
        xkb {}
        numlock
    }

    touchpad {
        tap
        dwt
        natural-scroll
        accel-speed 0.2
        accel-profile "flat"
        scroll-method "two-finger"
    }
}

// ============================================================================
//                             OUTPUTS
// ============================================================================
output "eDP-1" {
    mode "1920x1080@60.008"
    scale 1.0
    position x=0 y=0
}

output "HDMI-A-1" {
    mode "1920x1080@60.000"
    scale 1.0
    position x=0 y=-1080
    // position x=1920 y=0 // this is for horizontal movement
}

// ============================================================================
//                              LAYOUT
// ============================================================================
layout {
    gaps 6
    center-focused-column "never"

    preset-column-widths {
        proportion 0.33333
        proportion 0.5
        proportion 0.66667
    }

    preset-window-heights {
        proportion 0.33333
        proportion 0.5
        proportion 0.66667
    }

    default-column-width { proportion 0.5; }

    focus-ring {
        width 2.5
        inactive-color "#505050"
        active-gradient from="#B0E4CC" to="#8BAE66" angle=45
    }

    border { off; }
    shadow { off; }
}

// ============================================================================
//                         APLICACIONES AL INICIO
// ============================================================================
prefer-no-csd

hotkey-overlay {
    skip-at-startup
}

screenshot-path "~/Imágenes/Screenshots/Screenshot from %Y-%m-%d %H-%M-%S.png"

spawn-at-startup "wlsunset" "-T" "3001" "-t" "3000"
spawn-at-startup "waybar"
spawn-at-startup "swaync"
spawn-at-startup "/home/sbi/.local/bin/niri_wallpaper.sh"
spawn-at-startup "/home/sbi/.local/bin/niri-idle.sh"
spawn-at-startup "/home/sbi/.local/bin/low-battery-notify.sh"

// ============================================================================
//                             ANIMACIONES
// ============================================================================
animations {}

// ============================================================================
//                         REGLAS DE VENTANAS
// ============================================================================

// -- Esquinas redondeadas (global) --
window-rule {
    geometry-corner-radius 8
    clip-to-geometry true
}

// -- Qalculate! (Calculadora) --
window-rule {
    match app-id=r#"^qalculate-gtk$"#
    open-floating true
    open-focused true
    default-column-width { fixed 800; }
    default-window-height { fixed 750; }
}

// -- Diálogos GTK (Abrir/Salvar) --
window-rule {
    match app-id=r#"xdg-desktop-portal-gtk"#
    default-column-width { fixed 900; }
    default-window-height { fixed 900; }
    open-floating true
    open-focused true
}

// -- Pavucontrol (Audio) --
window-rule {
    match app-id=r#"^org\.pulseaudio\.pavucontrol$"#
    open-floating true
    default-column-width { fixed 950; }
    default-window-height { fixed 700; }
    open-focused true
}

// -- Blueman (Bluetooth) --
window-rule {
    match app-id=r#"^blueman-manager$"#
    open-floating true
    default-column-width { fixed 745; }
    default-window-height { fixed 745; }
    open-focused true
}

// -- Thunar: diálogos de propiedades --
window-rule {
    match app-id=r#"^thunar$"# title="^Properties$"
    match app-id=r#"^thunar$"# title="^Compress$"
    match app-id=r#"^thunar$"# title="^Rename$"
    match app-id=r#"^thunar$"# title="^Bulk Rename$"
    open-floating true
}

// -- Thunar: progreso de copiar/mover --
window-rule {
    match app-id=r#"^thunar$"# title="^File Operation Progress$"
    open-floating true
    default-column-width { proportion 0.4; }
}

// -- Picture-in-Picture (navegadores) --
window-rule {
    match app-id=r#"firefox$"# title="^Picture-in-Picture$"
    match app-id="librewolf$" title="^Picture-in-Picture$"
    match app-id="org.mozilla.firefox" title="^Picture-in-Picture$"
    match app-id="org.librewolf.LibreWolf" title="^Picture-in-Picture$"
    match app-id="com.github.Eloston.UngoogledChromium" title="^Picture-in-Picture$"
    match app-id="org.chromium.Chromium" title="^Picture-in-Picture$"
    match title="^LibreWolf - Seleccione un perfil$"
    match title="LibreWolf - Choose a profile"
    match title="^Catálogo$"
    open-floating true
    open-focused true
}

// -- Privacidad: bloquear capturas de KeePassXC --
window-rule {
    match app-id=r#"^org\.keepassxc\.KeePassXC$"#
    block-out-from "screen-capture"
}

// -- Diálogos de impresión --
window-rule {
    match title="^Print$"
    match title="^Imprimir$"
    open-floating true
    open-focused true
}

// ============================================================================
//                               BINDS
// ============================================================================
binds {

    // ====================================================================
    //                     APLICACIONES
    // ====================================================================

    // Terminales
    Mod+Return              hotkey-overlay-title="Open a Terminal: Kitty" { spawn "kitty"; }
    Mod+BackSpace           hotkey-overlay-title="Open nvim in WORKDIR" { spawn "kitty" "-d" "~/turtleShell" "-e" "tmux" "new" "-s" "turtleShell" "nvim"; }

    // Lanzadores
    Mod+D                   hotkey-overlay-title="Run an Application: fuzzel" { spawn "fuzzel"; }
    Mod+F1                  hotkey-overlay-title="Run Fuzzel In dmenu MODE" { spawn "~/.local/bin/dmenu"; }
    Mod+Space               hotkey-overlay-title="File Manager: Thunar" { spawn-sh "thunar"; }
    Mod+E                   hotkey-overlay-title="Emoji Picker" { spawn "/home/sbi/.local/bin/emojipicker.sh"; }

    // Seguridad
    Mod+Shift+Q             hotkey-overlay-title="Lock the Screen: swaylock" { spawn "/home/sbi/.local/bin/lock-screen"; }

    // Utilidades
    Mod+Shift+T             hotkey-overlay-title="Toggle ON/OFF AutoSuspend" { spawn "~/.local/bin/toggle-idle.sh"; }
    Mod+N                   hotkey-overlay-title="Toggle ON/OFF Visual Protect" { spawn "sh" "-c" "pgrep wlsunset && pkill wlsunset || wlsunset -T 3001 -t 3000"; }
    Mod+W                   hotkey-overlay-title="Open Wallpaper Change Menu" { spawn "/home/sbi/.local/bin/fuzzel-Wallpaper"; }
    Mod+Shift+W             hotkey-overlay-title="Change Wallpaper Randomly" { spawn "sh" "-c" "rm -f /tmp/niri_wallpaper.lock; /home/sbi/.local/bin/niri_wallpaper.sh"; }

    // Overlay
    Mod+F5 { show-hotkey-overlay; }

    // ====================================================================
    //                     VOLUMEN Y MULTIMEDIA
    // ====================================================================

    XF86AudioRaiseVolume    allow-when-locked=true { spawn-sh "wpctl set-volume @DEFAULT_AUDIO_SINK@ 0.05+ -l 1.0"; }
    XF86AudioLowerVolume    allow-when-locked=true { spawn-sh "wpctl set-volume @DEFAULT_AUDIO_SINK@ 0.05-"; }
    XF86AudioMute           allow-when-locked=true { spawn-sh "wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle"; }
    XF86AudioMicMute        allow-when-locked=true { spawn-sh "wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle"; }
    XF86AudioPlay           allow-when-locked=true { spawn-sh "playerctl play-pause"; }
    XF86AudioPause          allow-when-locked=true { spawn-sh "playerctl play-pause"; }
    XF86AudioStop           allow-when-locked=true { spawn-sh "playerctl stop"; }
    XF86AudioPrev           allow-when-locked=true { spawn-sh "playerctl previous"; }
    XF86AudioNext           allow-when-locked=true { spawn-sh "playerctl next"; }

    // ====================================================================
    //                         BRILLO
    // ====================================================================

    XF86MonBrightnessUp     allow-when-locked=true { spawn "brightnessctl" "--class=backlight" "set" "+10%"; }
    XF86MonBrightnessDown   allow-when-locked=true { spawn "brightnessctl" "--class=backlight" "set" "10%-"; }

    // ====================================================================
    //                    NAVEGACIÓN DE VENTANAS
    // ====================================================================

    Mod+O repeat=false { toggle-overview; }
    Mod+Q repeat=false { close-window; }

    // Foco: columnas
    Mod+Left                { focus-column-left; }
    Mod+Right               { focus-column-right; }
    Mod+H                   { focus-column-left; }
    Mod+L                   { focus-column-right; }

    // Foco: ventanas dentro de columna
    Mod+Down                { focus-window-down; }
    Mod+Up                  { focus-window-up; }
    Mod+J                   { focus-window-down; }
    Mod+K                   { focus-window-up; }

    // Mover columnas
    Mod+Ctrl+Left           { move-column-left; }
    Mod+Ctrl+Right          { move-column-right; }
    Mod+Ctrl+H              { move-column-left; }
    Mod+Ctrl+L              { move-column-right; }

    // Mover ventanas dentro de columna
    Mod+Ctrl+Down           { move-window-down; }
    Mod+Ctrl+Up             { move-window-up; }
    Mod+Ctrl+J              { move-window-down; }
    Mod+Ctrl+K              { move-window-up; }

    // ====================================================================
    //                           MONITORES
    // ====================================================================

    // Foco
    Mod+Shift+Left          { focus-monitor-left; }
    Mod+Shift+Right         { focus-monitor-right; }
    Mod+Shift+H             { focus-monitor-left; }
    Mod+Shift+L             { focus-monitor-right; }
    Mod+Shift+Down          { focus-monitor-down; }
    Mod+Shift+Up            { focus-monitor-up; }
    Mod+Shift+J             { focus-monitor-down; }
    Mod+Shift+K             { focus-monitor-up; }

    // Mover columna a monitor
    Mod+Shift+Ctrl+Left     { move-column-to-monitor-left; }
    Mod+Shift+Ctrl+Right    { move-column-to-monitor-right; }
    Mod+Shift+Ctrl+H        { move-column-to-monitor-left; }
    Mod+Shift+Ctrl+L        { move-column-to-monitor-right; }
    Mod+Shift+Ctrl+Down     { move-column-to-monitor-down; }
    Mod+Shift+Ctrl+Up       { move-column-to-monitor-up; }
    Mod+Shift+Ctrl+J        { move-column-to-monitor-down; }
    Mod+Shift+Ctrl+K        { move-column-to-monitor-up; }

    // ====================================================================
    //                         WORKSPACES
    // ====================================================================

    // Foco entre workspaces
    Mod+Page_Down           { focus-workspace-down; }
    Mod+Page_Up             { focus-workspace-up; }
    Mod+U                   { focus-workspace-down; }
    Mod+I                   { focus-workspace-up; }

    // Mover columna a workspace contiguo
    Mod+Ctrl+Page_Down      { move-column-to-workspace-down; }
    Mod+Ctrl+Page_Up        { move-column-to-workspace-up; }
    Mod+Ctrl+U              { move-column-to-workspace-down; }
    Mod+Ctrl+I              { move-column-to-workspace-up; }

    // Mover workspace entero
    Mod+Shift+Page_Down     { move-workspace-down; }
    Mod+Shift+Page_Up       { move-workspace-up; }
    Mod+Shift+U             { move-workspace-down; }
    Mod+Shift+I             { move-workspace-up; }

    // Foco por número
    Mod+1 { focus-workspace 1; }
    Mod+2 { focus-workspace 2; }
    Mod+3 { focus-workspace 3; }
    Mod+4 { focus-workspace 4; }
    Mod+5 { focus-workspace 5; }
    Mod+6 { focus-workspace 6; }
    Mod+7 { focus-workspace 7; }
    Mod+8 { focus-workspace 8; }
    Mod+9 { focus-workspace 9; }

    // Mover columna a workspace por número
    Mod+Shift+1 { move-column-to-workspace 1; }
    Mod+Shift+2 { move-column-to-workspace 2; }
    Mod+Shift+3 { move-column-to-workspace 3; }
    Mod+Shift+4 { move-column-to-workspace 4; }
    Mod+Shift+5 { move-column-to-workspace 5; }
    Mod+Shift+6 { move-column-to-workspace 6; }
    Mod+Shift+7 { move-column-to-workspace 7; }
    Mod+Shift+8 { move-column-to-workspace 8; }
    Mod+Shift+9 { move-column-to-workspace 9; }

    // ====================================================================
    //                           RATÓN
    // ====================================================================

    // Workspaces
    Mod+WheelScrollDown             cooldown-ms=149 { focus-workspace-down; }
    Mod+WheelScrollUp               cooldown-ms=149 { focus-workspace-up; }
    Mod+Ctrl+WheelScrollDown        cooldown-ms=149 { move-column-to-workspace-down; }
    Mod+Ctrl+WheelScrollUp          cooldown-ms=149 { move-column-to-workspace-up; }

    // Columnas
    Mod+WheelScrollRight            { focus-column-right; }
    Mod+WheelScrollLeft             { focus-column-left; }
    Mod+Ctrl+WheelScrollRight       { move-column-right; }
    Mod+Ctrl+WheelScrollLeft        { move-column-left; }

    Mod+Shift+WheelScrollDown       { focus-column-right; }
    Mod+Shift+WheelScrollUp         { focus-column-left; }
    Mod+Ctrl+Shift+WheelScrollDown  { move-column-right; }
    Mod+Ctrl+Shift+WheelScrollUp    { move-column-left; }

    // ====================================================================
    //                     GESTIÓN DE VENTANAS
    // ====================================================================

    // Comer/expulsar entre columnas
    Mod+BracketLeft         { consume-or-expel-window-left; }
    Mod+BracketRight        { consume-or-expel-window-right; }
    Mod+Comma               { consume-window-into-column; }
    Mod+Period              { expel-window-from-column; }

    // Tamaños
    Mod+R                   { switch-preset-column-width; }
    Mod+Shift+R             { switch-preset-column-width-back; }
    Mod+Ctrl+Shift+R        { switch-preset-window-height; }
    Mod+Ctrl+R              { reset-window-height; }

    // Ajuste fino
    Mod+Minus               { set-column-width "-10%"; }
    Mod+Plus                { set-column-width "+10%"; }
    Mod+Shift+Minus         { set-window-height "-10%"; }
    Mod+Shift+Plus          { set-window-height "+10%"; }

    // Maximizar / pantalla completa
    Mod+F                   { maximize-column; }
    Mod+Shift+F             { fullscreen-window; }
    Mod+M                   { maximize-window-to-edges; }
    Mod+Ctrl+F              { expand-column-to-available-width; }

    // Centrar
    Mod+C                   { center-column; }
    Mod+Ctrl+C              { center-visible-columns; }

    // Modo flotante
    Mod+V                   { toggle-window-floating; }
    Mod+Shift+V             { switch-focus-between-floating-and-tiling; }

    // Tabbed
    Mod+B                   { toggle-column-tabbed-display; }

    // ====================================================================
    //                     CAPTURAS DE PANTALLA
    // ====================================================================

    Print                    { screenshot; }
    Ctrl+Print               { screenshot-screen; }
    Alt+Print                { screenshot-window; }

    // ====================================================================
    //                          SISTEMA
    // ====================================================================

    Mod+Escape allow-inhibiting=false { toggle-keyboard-shortcuts-inhibit; }
    Mod+Shift+Escape         { quit; }
    Ctrl+Alt+Delete          { quit; }
    Mod+Shift+P              { power-off-monitors; }
}
